{
  "name": "vue-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:major": "git add . && git commit -m \"Pre-version commit\" || true && npm version major",
    "generate:minor": "git add . && git commit -m \"Pre-version commit\" || true && npm version minor",
    "generate:patch": "git add . && git commit -m \"Pre-version commit\" || true && npm version patch",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "vite": "^5.3.1"
  }
}
